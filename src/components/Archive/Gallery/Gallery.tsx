import React from "react";

const Gallery: React.FC = () => {
  return (
    <main style={{ padding: "2rem" }}>
      <h1>Gallery</h1>
      <p>Showcase all galleries here, maybe with filters or categories.</p>
    </main>
  );
};

export default Gallery;